import { DailyAnimationDirective } from './daily-animation.directive';

describe('DailyAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new DailyAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
