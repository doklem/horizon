import { DayTimeAnimationsDirective } from './day-time-animations.directive';

describe('DayTimeAnimationsDirective', () => {
  it('should create an instance', () => {
    const directive = new DayTimeAnimationsDirective();
    expect(directive).toBeTruthy();
  });
});
