import { DawnDuskAnimationDirective } from './dawn-dusk-animation.directive';

describe('DawnDuskAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new DawnDuskAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
