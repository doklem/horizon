import { CelestialBodiesAnimationDirective } from './celestial-bodies-animation.directive';

describe('CelestialBodiesAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new CelestialBodiesAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
