import { ContourLineRequest } from './contour-line-request';

describe('ContourLineRequest', () => {
  it('should create an instance', () => {
    expect(new ContourLineRequest()).toBeTruthy();
  });
});
