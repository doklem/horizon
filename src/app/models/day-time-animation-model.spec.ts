import { DayTimeAnimationModel } from './day-time-animation-model';

describe('DayTimeAnimationModel', () => {
  it('should create an instance', () => {
    expect(new DayTimeAnimationModel()).toBeTruthy();
  });
});
