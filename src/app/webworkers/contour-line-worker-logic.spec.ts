import { ContourLineWorkerLogic } from './contour-line-worker-logic';

describe('ContourLineWorkerLogic', () => {
  it('should create an instance', () => {
    expect(new ContourLineWorkerLogic()).toBeTruthy();
  });
});
